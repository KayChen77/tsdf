#pragma once

struct FrameInfo
{
	int id;
	// F2W
	float scale;
	Eigen::Matrix3f rotMatUnscaled;
	Eigen::Vector3f transVec;
	pcl::device::Intr intr = pcl::device::Intr (366.996, 366.996, 256.466, 209.012);
};
